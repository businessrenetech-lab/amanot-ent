node_modules/
build/
coverage/
.DS_Store
*.log
.env*
!.env.example
.gstack/
.antigravity/

# Build output IS committed so the host can run without a build step.
# Regenerate with: VITE_API_BASE= npm run build && npm run build:server
